{
  "name": "WallpaperCluster Dev (Deno + Solid + Vite)",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:1-20-bullseye",
  "customizations": {
    "vscode": {
      "extensions": [
        "denoland.vscode-deno",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "1yib.solid-js-snippets"
      ],
      "settings": {
        "deno.enable": true,
        "editor.formatOnSave": true
      }
    }
  },
  "forwardPorts": [3000],
  "postCreateCommand": "npm install -g deno && npm install -g jsx && deno install && npm install -g sass",
  "postStartCommand": "npm install -g npm@latest && npm fund && npm -v && deno add npm:sass-embedded && deno task dev"
}